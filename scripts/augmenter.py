"""
DEPRECATED — This script is no longer used for data generation.
Synthetic data (nuanced_generated, vast_expansion) is excluded from training
in build_dataset_from_csv(exclude_synthetic=True).

The original script generated 15,000 synthetic utterances that polluted
the dataset with single-turn template dialogues. Retained for audit only.
"""
print("DEPRECATED: This script no longer generates data. See header comment.")
